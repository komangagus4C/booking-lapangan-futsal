import 'package:flutter/material.dart';

class jadwalbooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('jadwal booking'),
      ),
      body: Center(
        child: Text('Welcome '),
      ),
    );
  }
}


